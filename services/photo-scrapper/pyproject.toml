[project]
name = "photo-scrapper"
version = "0.1.0"
dependencies = [
  "dagster",
  "dagster-webserver",
  "pyyaml",
  "azure-servicebus",
  "python-dotenv"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
